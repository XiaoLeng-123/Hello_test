server:
  port: 9003
  
  
spring:
  application:
    name: nacos-config9003
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848    #配置nacos的地址
      config:        server-addr: localhost:8848   #nacos作为配置中心地址
        file-extension: yaml
    
    
management:
  endpoints:
    web:
      exposure:
        include: "*"    